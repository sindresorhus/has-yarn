/**
 * Check if a project is using [Yarn](https://yarnpkg.com/).
 *
 * @param cwd - Current working directory. Default: `process.cwd()`.
 * @returns Whether the project uses `yarn` or not.
 */
export default function hasYarn(cwd?: string): boolean;
